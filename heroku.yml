build:
  docker:
    web: Dockerfile
    # server: ./client/Dockerfile

# release:
#   command: 
#     - npm start
#   image: server

#   command:
#     - npm start
#   image: client

run:
  web: npm start
  # server: npm start

# release:
#   image: server
#   command: 
#     - npm start up

# release:
#   image: web
#   command: 
#     - npm start up
