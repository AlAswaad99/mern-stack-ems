build:
  docker:
    server: ./server/Dockerfile
    client: ./client/Dockerfile

release:
  command: 
    - npm start
  image: server

  command:
    - npm start
  image: client

run:
  server: npm start
  client: npm start
