build:
  docker:
    web: ./client/Dockerfile
    server: ./server/Dockerfile

# release:
#   command: 
#     - npm start
#   image: server

#   command:
#     - npm start
#   image: client

run:
  web: npm run build
  server: npm start

# release:
#   image: server
#   command: 
#     - npm start up

# release:
#   image: web
#   command: 
#     - npm start up
