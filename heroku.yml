build:
  docker:
    server: ./server/Dockerfile
    web: ./client/Dockerfile

# release:
#   command: 
#     - npm start
#   image: server

#   command:
#     - npm start
#   image: client

run:
  server: NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client
  web: npm start

# release:
#   command: 
#     - npm run migrate up
#   image: web
